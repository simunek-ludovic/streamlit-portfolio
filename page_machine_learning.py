# PAGE ml
# fichier d'appui si besoin :
# 

# Contenu en fonction du choix dans le sous-menu
st.write("Le ***Machine Learning*** est une branche de l'intelligence artificielle qui permet à un ordinateur d'apprendre à partir de données, sans être explicitement programmé pour chaque tâche.")
st.write("Il utilise des algorithmes pour identifier des modèles ou des structures dans les données et effectuer des prédictions ou des décisions basées sur ces informations.")
st.write("En résumé, c'est l'art de faire apprendre une machine à partir d'exemples pour qu'elle puisse prendre des décisions ou effectuer des tâches par elle-même.")

# Créer un trait de séparation
st.markdown("""
    <style>
        hr {
            border: 1px solid #000000;  /* couleur du trait */
            margin: 1px 0;             /* espace avant et après le trait */
            height: 5px;                /* hauteur du trait */
        }
    </style>
    <hr>
    """, unsafe_allow_html=True)

st.write("La régression linéaire simple est un modèle statistique utilisé pour analyser la relation entre deux variables quantitatives.")
st.write("Elle permet de prédire la valeur d'une variable dépendante (y) en fonction de la valeur d'une variable indépendante (X)")

# Créer un trait de séparation
st.markdown("""
    <style>
        hr {
            border: 1px solid #000000;  /* couleur du trait */
            margin: 1px 0;             /* espace avant et après le trait */
            height: 5px;                /* hauteur du trait */
        }
    </style>
    <hr>
    """, unsafe_allow_html=True)

st.write("La régression linéaire multiple est une extension de la régression linéaire simple qui permet d'analyser la relation entre une variable dépendante (y) et plusieurs variables indépendantes (X)")
st.write("Cela permet de prendre en compte plusieurs facteurs pour mieux expliquer ou prédire y")
